# Database connection string (e.g. postgres://user:password@host:port/dbname)
POSTGRES_URL=
# Set to "true" if your provider requires SSL (common in managed databases)
POSTGRES_SSL=true

# Email notifications via Resend
RESEND_API_KEY=
NOTIFY_EMAIL_FROM=Basix <no-reply@example.com>
NOTIFY_EMAIL_TO=
